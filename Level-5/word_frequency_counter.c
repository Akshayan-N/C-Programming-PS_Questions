// Depending 
//This is let to be implemented , so Skip this one
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main()
{
    char sentence[100];
    printf("Enter the sentence : ");
    fgets(sentence, 100, stdin);

    int len = strlen(sentence)
    char word[len] = "";
    int j = 0; 
    for (int i = 0; i < len; i++)
    {
        if (isalpha(letter))
        {
            word[j++] = letter;
        }
        else
        {
            // implement hash using two array one for count and one for word
            word = ""
            j = 0;
        }
        
    }
}